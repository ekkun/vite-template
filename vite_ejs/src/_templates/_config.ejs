<%_;
  if (environment === 'production') {
    // 本番環境
    domain = 'https://production.com';
    path = '/';
    dir = '/';
  } else if (environment === 'development') {
    // 開発環境
    domain = 'https://development.com/sub-directory';
    path = '/sub-directory/';
    dir = '/sub-directory/';
  } else {
    // ローカル環境
    domain = 'http://localhost:4000';
    path = '/';
    dir = '/';
  }
_%>
